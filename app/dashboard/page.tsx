'use client'
import {CrmShell} from '@/components/crm-shell';import {demo,rupiah} from '@/lib/api';import {ArrowUpRight,Users,Package,FileText,Wallet,Plus} from 'lucide-react';import Link from 'next/link';
const stats: Array<[string,string|number,typeof Users,string]>=[['Total Customer',demo.customers.length,Users,'+12.5%'],['Produk Aktif',demo.products.filter(p=>p.is_active).length,Package,'+8.2%'],['Invoice Bulan Ini',demo.invoices.length,FileText,'+18.4%'],['Pendapatan Bulan Ini',rupiah(demo.invoices.reduce((a,b)=>a+b.net_amount,0)),Wallet,'+24.8%']];export default function Dashboard(){return <CrmShell><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm font-medium text-primary">Senin, 1 September 2026</p><h1 className="mt-2 font-display text-4xl font-bold tracking-tight">Selamat pagi, Admin.</h1><p className="mt-2 text-muted-foreground">Berikut ringkasan aktivitas bisnis Anda hari ini.</p></div><Link href="/invoices/new" className="flex items-center gap-2 rounded-xl bg-primary px-4 py-3 text-sm font-semibold text-primary-foreground"><Plus size={18}/>Buat Invoice</Link></div><div className="mt-10 grid gap-5 sm:grid-cols-2 xl:grid-cols-4">{stats.map(([label,value,Icon,change])=><div key={label as string} className="rounded-2xl border bg-card p-5 shadow-soft"><div className="flex items-center justify-between"><div className="grid size-11 place-items-center rounded-xl bg-accent text-primary"><Icon size={21}/></div><span className="rounded-full bg-accent px-2 py-1 text-xs font-semibold text-primary">{change}</span></div><p className="mt-6 text-sm text-muted-foreground">{label}</p><p className="mt-1 text-2xl font-bold">{value}</p></div>)}</div><div className="mt-8 grid gap-6 xl:grid-cols-[1.4fr_1fr]"><section className="rounded-2xl border bg-card p-6 shadow-soft"><div className="flex items-center justify-between"><div><h2 className="text-lg font-bold">Invoice Terbaru</h2><p className="text-sm text-muted-foreground">Pantau transaksi terbaru Anda</p></div><Link className="text-sm font-semibold text-primary" href="/invoices">Lihat semua</Link></div><div className="mt-6 flex flex-col gap-1">{demo.invoices.map(i=><div key={i.id} className="flex items-center justify-between rounded-xl px-3 py-4 hover:bg-muted"><div><p className="font-semibold">{i.invoice_number}</p><p className="text-sm text-muted-foreground">{i.customer_name} · {i.invoice_date}</p></div><div className="text-right"><p className="font-semibold">{rupiah(i.net_amount)}</p><span className="text-xs font-medium text-primary">{i.status_transaksi}</span></div></div>)}</div></section><section className="rounded-2xl border bg-card p-6 shadow-soft"><div className="flex items-center justify-between"><div><h2 className="text-lg font-bold">Produk Menipis</h2><p className="text-sm text-muted-foreground">Perlu perhatian segera</p></div><Link href="/products" className="text-primary"><ArrowUpRight size={20}/></Link></div><div className="mt-6 flex flex-col gap-4">{demo.products.filter(p=>p.stock<10).map(p=><div key={p.id} className="flex items-center justify-between"><div><p className="font-semibold">{p.sku}</p><p className="text-sm text-muted-foreground">{p.category}</p></div><span className="rounded-full bg-red-50 px-3 py-1 text-xs font-semibold text-red-600">{p.stock} tersisa</span></div>)}</div></section></div></CrmShell>}

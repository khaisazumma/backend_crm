'use client'
import {useState} from 'react';import {useRouter} from 'next/navigation';import {toast} from 'sonner';import {api} from '@/lib/api';
export default function Login(){const r=useRouter(),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);try{const d=await api<any>('/auth/login',{method:'POST',body:JSON.stringify({email,password})});localStorage.setItem('venambak_token',d.access_token);localStorage.setItem('venambak_admin',JSON.stringify(d.admin));toast.success('Selamat datang kembali');r.push('/dashboard')}catch(e){toast.error(e instanceof Error?e.message:'Login gagal')}finally{setBusy(false)}}return <main className="grid min-h-screen place-items-center bg-background p-5"><div className="w-full max-w-md rounded-3xl border bg-card p-8 shadow-soft md:p-10"><div className="mb-10 flex items-center gap-3"><div className="grid size-12 place-items-center rounded-2xl bg-primary text-2xl font-bold text-primary-foreground">V</div><div><h1 className="font-display text-2xl font-bold">Venambak</h1><p className="text-xs tracking-widest text-muted-foreground">CRM PLATFORM</p></div></div><h2 className="font-display text-3xl font-bold">Selamat datang</h2><p className="mt-2 text-sm text-muted-foreground">Masuk untuk mengelola bisnis tambak Anda.</p><form onSubmit={submit} className="mt-8 flex flex-col gap-5"><label className="flex flex-col gap-2 text-sm font-semibold">Email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="admin@venambak.id" className="rounded-xl border bg-background px-4 py-3 font-normal outline-none focus:ring-2 focus:ring-primary"/></label><label className="flex flex-col gap-2 text-sm font-semibold">Password<input required type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Masukkan password" className="rounded-xl border bg-background px-4 py-3 font-normal outline-none focus:ring-2 focus:ring-primary"/></label><button disabled={busy} className="rounded-xl bg-primary px-4 py-3 font-semibold text-primary-foreground disabled:opacity-60">{busy?'Memproses...':'Masuk ke Dashboard'}</button></form></div></main>}

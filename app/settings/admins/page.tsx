'use client'
import {CrmShell} from '@/components/crm-shell';import {useState} from 'react';import {api} from '@/lib/api';import {toast} from 'sonner';
export default function Admins(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[role,setRole]=useState('SALES'),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);try{await api('/auth/admins',{method:'POST',body:JSON.stringify({email,password,role})});toast.success('Admin berhasil dibuat');setEmail('');setPassword('')}catch(e){toast.error(e instanceof Error?e.message:'Gagal membuat admin')}finally{setBusy(false)}}return <CrmShell><h1 className="font-display text-4xl font-bold">Admin Management</h1><p className="mt-2 text-muted-foreground">Tambahkan anggota tim untuk mengelola Venambak CRM.</p><div className="mt-8 max-w-xl rounded-2xl border bg-card p-6 shadow-soft"><h2 className="text-lg font-bold">Tambah Admin Baru</h2><form onSubmit={submit} className="mt-6 flex flex-col gap-5"><label className="flex flex-col gap-2 text-sm font-semibold">Email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} className="rounded-xl border bg-background px-4 py-3 font-normal" placeholder="nama@venambak.id"/></label><label className="flex flex-col gap-2 text-sm font-semibold">Password<input required minLength={8} type="password" value={password} onChange={e=>setPassword(e.target.value)} className="rounded-xl border bg-background px-4 py-3 font-normal" placeholder="Minimal 8 karakter"/></label><label className="flex flex-col gap-2 text-sm font-semibold">Role<select value={role} onChange={e=>setRole(e.target.value)} className="rounded-xl border bg-background px-4 py-3 font-normal"><option>SALES</option><option>ADMIN</option><option>SUPER_ADMIN</option></select></label><button disabled={busy} className="rounded-xl bg-primary px-4 py-3 font-semibold text-primary-foreground disabled:opacity-60">{busy?'Menyimpan...':'Buat Admin'}</button></form></div></CrmShell>}

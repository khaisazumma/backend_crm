'use client'
import {usePathname,useRouter} from 'next/navigation';import {LayoutDashboard,Users,Package,FileText,Wallet,Settings,LogOut,Menu,Search,Bell} from 'lucide-react';import {useState} from 'react';
const links=[['/dashboard','Dashboard',LayoutDashboard],['/customers','Customers',Users],['/products','Products',Package],['/invoices','Invoices',FileText],['/commissions','Commissions',Wallet],['/settings/admins','Settings',Settings]] as const;
export function CrmShell({children}:{children:React.ReactNode}){const path=usePathname(),router=useRouter(),[open,setOpen]=useState(false);const admin=typeof window!=='undefined'?JSON.parse(localStorage.getItem('venambak_admin')||'{"email":"admin@venambak.id","role":"ADMIN"}'):null;return <div className="min-h-screen bg-background"><aside className={`fixed inset-y-0 left-0 z-40 w-64 border-r bg-card p-5 transition-transform md:translate-x-0 ${open?'translate-x-0':'-translate-x-full'}`}><div className="mb-10 flex items-center gap-3"><div className="grid size-10 place-items-center rounded-xl bg-primary text-xl font-bold text-primary-foreground">V</div><div><div className="font-display text-xl font-bold">Venambak</div><div className="text-xs text-muted-foreground">CRM PLATFORM</div></div></div><nav className="flex flex-col gap-2">{links.map(([href,label,Icon])=><button key={href} onClick={()=>{router.push(href);setOpen(false)}} className={`flex items-center gap-3 rounded-xl px-4 py-3 text-left text-sm font-medium transition ${path.startsWith(href)?'bg-accent text-primary':'text-muted-foreground hover:bg-muted'}`}><Icon size={19}/>{label}</button>)}</nav><button onClick={()=>{localStorage.clear();router.push('/login')}} className="absolute bottom-6 left-5 flex items-center gap-3 rounded-xl px-4 py-3 text-sm text-muted-foreground"><LogOut size={18}/>Keluar</button></aside><div className="md:pl-64"><header className="flex h-20 items-center justify-between border-b bg-card/80 px-5 md:px-10"><button className="md:hidden" onClick={()=>setOpen(true)}><Menu/></button><div className="hidden items-center gap-3 rounded-xl bg-muted px-4 py-2 text-sm text-muted-foreground sm:flex"><Search size={17}/>Cari apa saja...</div><div className="flex items-center gap-5"><Bell size={19} className="text-muted-foreground"/><div className="hidden text-right sm:block"><div className="text-sm font-semibold">{admin?.email||'Admin'}</div><div className="text-xs text-muted-foreground">{admin?.role||'ADMIN'}</div></div><div className="grid size-10 place-items-center rounded-full bg-accent font-bold text-primary">A</div></div></header><main className="p-5 md:p-10">{children}</main></div></div>}

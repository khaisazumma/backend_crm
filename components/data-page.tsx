'use client'
import {CrmShell} from '@/components/crm-shell';import {demo,rupiah} from '@/lib/api';import {Search,Plus,ChevronLeft,ChevronRight} from 'lucide-react';import {usePathname} from 'next/navigation';import Link from 'next/link';
export default function DataPage(){const path=usePathname();const type=path.split('/')[1];const config=type==='customers'?{title:'Customers',desc:'Kelola data pelanggan dan tambak Anda.',button:'Tambah Customer',headers:['Nama','Telepon','Email','Kabupaten/Kota','Komoditas'],rows:demo.customers.map(x=>[x.name,x.phone,x.email,x.city,x.commodity])}:type==='products'?{title:'Products',desc:'Kelola katalog produk dan stok Anda.',button:'Tambah Produk',headers:['SKU','Jenis','Kategori','Harga Normal','Stok'],rows:demo.products.map(x=>[x.sku,x.product_type,x.category,rupiah(x.normal_price),x.stock])}:{title:'Invoices',desc:'Pantau seluruh transaksi dan status pesanan.',button:'Buat Invoice Baru',headers:['Nomor Invoice','Customer','Tanggal','Nilai','Status'],rows:demo.invoices.map(x=>[x.invoice_number,x.customer_name,x.invoice_date,rupiah(x.net_amount),x.status_transaksi])};return <CrmShell><div className="flex flex-wrap items-end justify-between gap-4"><div><h1 className="font-display text-4xl font-bold">{config.title}</h1><p className="mt-2 text-muted-foreground">{config.desc}</p></div><Link href="#" className="flex items-center gap-2 rounded-xl bg-primary px-4 py-3 text-sm font-semibold text-primary-foreground"><Plus size={18}/>{config.button}</Link></div><div className="mt-8 overflow-hidden rounded-2xl border bg-card shadow-soft"><div className="flex flex-wrap items-center justify-between gap-4 border-b p-5"><div className="flex items-center gap-3 rounded-xl bg-muted px-4 py-2 text-sm text-muted-foreground"><Search size={17}/><input placeholder="Cari data..." className="w-48 bg-transparent outline-none"/></div><span className="text-sm text-muted-foreground">{config.rows.length} data ditemukan</span></div><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-muted text-xs uppercase tracking-wide text-muted-foreground"><tr>{config.headers.map(h=><th key={h} className="px-6 py-4 font-semibold">{h}</th>)}</tr></thead><tbody>{config.rows.map((row,i)=><tr key={i} className="border-t hover:bg-muted/50">{row.map((cell,j)=><td key={j} className={`px-6 py-5 ${j===0?'font-semibold':''}`}>{cell}</td>)}</tr>)}</tbody></table></div><div className="flex items-center justify-between border-t p-4 text-sm text-muted-foreground"><span>Menampilkan 1-{config.rows.length}</span><div className="flex gap-2"><button className="rounded-lg border p-2"><ChevronLeft size={16}/></button><button className="rounded-lg border p-2"><ChevronRight size={16}/></button></div></div></div></CrmShell>}
